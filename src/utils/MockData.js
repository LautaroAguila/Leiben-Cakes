import { db } from "../firebase/config";

export const getProducts = () =>{
    return new Promise ((resolve, reject) => {
        setTimeout(()=>{
            resolve(products);
        }, );
    });
};
